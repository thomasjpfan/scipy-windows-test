name: Test Scipy
on: [push]

defaults:
  run:
    shell: bash

jobs:
  test_scipy:
    runs-on: windows-latest
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v2
      - run: docker build -t test_build .
      - run: docker run test_build python -c "from scipy.sparse import csr_matrix"
